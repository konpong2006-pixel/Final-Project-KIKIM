{
  "name": "smartlife-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && npx -y firebase-tools@latest deploy --only functions",
    "lint": "tsc --noEmit",
    "test:ocr": "npm run build && node scripts/test-ocr-pipeline.cjs",
    "test:adaptive": "npm run build && node scripts/test-adaptive-scheduling.cjs",
    "test:adaptive-transactions:local": "npm run build && node scripts/test-adaptive-transactions.cjs"
  },
  "dependencies": {
    "@google-cloud/vision": "^5.3.3",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0",
    "jpeg-js": "^0.4.4",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "@types/pngjs": "^6.0.5",
    "typescript": "^5.8.3"
  }
}
